<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant List</title>
  <!-- link css files here -->
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
</head>
<body>
  <header>
    <!-- navigation -->
    <nav class="navbar navbar-light bg-light" >
      <div class="container d-flex justify-content-between " style="max-width:2000px;margin:0px;">
        <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單">
          {{#if isAuthenticated}}{{ user.name}}'s {{/if}}餐廳清單
        </a>
          {{#if isAuthenticated}}
          <div class="d-flex">
            <a href="/restaurants/new" class="d-inline-block"
              style="text-decoration:none; color:gray; width:100px;font-size:20px; text-align: center; padding:5px 0px">新增餐廳</a>
            <a href="/users/logout" class="btn" style="margin-left:20px;border:0px;padding:5px 0px;font-size:20px;text-decoration:none; color:gray;">登出</a>
          </div>
          {{else}}
          <a href="/users/login" class="btn" style="margin-left:20px;border:0px;padding:5px 0px;font-size:20px;text-decoration:none; color:gray;">登入</a>
          {{/if}}
      </div>
    </nav>
  
    <div class="banner"></div>
  </header>


  <!--partial template -->
  {{{body}}}

  <!-- include javascript files here -->
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
  <script src="https://kit.fontawesome.com/3e50ed94d1.js" crossorigin="anonymous"></script>
</body>
</html>